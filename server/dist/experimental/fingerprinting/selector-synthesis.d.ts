import type { ScoreHit } from './types';
/**
 * Build a selector for the element a `ScoreHit` describes, or `null` when the
 * hit carries nothing trustworthy enough to select on.
 */
export declare function selectorFromHit(hit: ScoreHit): string | null;
//# sourceMappingURL=selector-synthesis.d.ts.map